import React from 'react';
import PropTypes from 'prop-types';

const Footer = props => {
    return (
        <footer>
            <p>©2019</p>
        </footer>
    );
};

Footer.propTypes = {
    
};

export default Footer;